# Copy to .env.local for local development. Do not commit real secrets.
# This controls the optional client-side admin label. Firestore writes still require
# the Firebase custom admin claim assigned by scripts/setAdminClaim.js.
VITE_ADMIN_EMAIL=admin@example.com
